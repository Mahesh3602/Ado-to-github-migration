name: "1. Migrate Repo & Export Mannequins"

on:
  workflow_dispatch:
    inputs:
      ado_repo_url:
        description: 'Azure DevOps Repo URL'
        required: true
      github_repo_name:
        description: 'New GitHub Repo Name'
        required: true

jobs:
  migration_job:
    runs-on: ubuntu-latest
    env:
      # These must match the names in your GitHub Repo Secrets
      GH_PAT: ${{ secrets.GH_PAT }}
      ADO_PAT: ${{ secrets.ADO_PAT }}
      GITHUB_ORG_NAME: ${{ secrets.ORG_NAME }}
      ADO_REPO_URL: ${{ github.event.inputs.ado_repo_url }}
      GIT_REPO_NAME: ${{ github.event.inputs.github_repo_name }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: 🔑 Grant Permissions
        run: chmod +x scripts/*.sh

      - name: 🛠️ Get Org & Source IDs
        run: |
          echo "--- 1. Fetching Org ID ---"
          ORG_RAW=$(./scripts/01-get_org_id.sh 2>&1)
          echo "$ORG_RAW"
          # Extract ID using grep and jq
          ORG_ID=$(echo "$ORG_RAW" | grep -o '{.*}' | jq -r '.data.organization.id // empty')

          echo "--- 2. Fetching Source ID ---"
          SOURCE_RAW=$(./scripts/02-create_migration_source_id.sh 2>&1)
          echo "$SOURCE_RAW"
          # Extract ID using grep and jq
          SOURCE_ID=$(echo "$SOURCE_RAW" | grep -o '{.*}' | jq -r '.data.createMigrationSource.migrationSource.id // empty')

          # Final Validation
          if [ -z "$ORG_ID" ] || [ "$ORG_ID" == "null" ]; then
            echo "❌ ERROR: Captured ORG_ID is empty."
            exit 1
          fi

          if [ -z "$SOURCE_ID" ] || [ "$SOURCE_ID" == "null" ]; then
            echo "❌ ERROR: Captured SOURCE_ID is empty."
            exit 1
          fi

          # Save to GitHub Environment
          echo "ORG_ID=$ORG_ID" >> $GITHUB_ENV
          echo "SOURCE_ID=$SOURCE_ID" >> $GITHUB_ENV
          echo "✅ IDs successfully saved: Org=$ORG_ID, Source=$SOURCE_ID"      - name: 🚀 Start Migration
        run: |
          # Execute migration and capture output
          ./scripts/03-start_migrate_repo.sh > migration_output.json || true
          
          echo "--- Debug: API Response ---"
          cat migration_output.json
          echo "---------------------------"

          RM_ID=$(cat migration_output.json | grep -o '{.*}' | jq -r '.data.startRepositoryMigration.repositoryMigration.id // empty')
          
          if [ -z "$RM_ID" ] || [ "$RM_ID" == "null" ]; then
            echo "❌ Migration failed to start. Check API response above."
            exit 1
          fi

          echo "MIGRATION_ID=$RM_ID" >> $GITHUB_ENV
          echo "🚀 Migration Started: $RM_ID"

      - name: ⏳ Wait for Completion
        run: |
          STATUS="QUEUED"
          while [[ "$STATUS" != "SUCCEEDED" && "$STATUS" != "FAILED" ]]; do
            RESPONSE=$(./scripts/04-check_status.sh)
            STATUS=$(echo "$RESPONSE" | grep -o '{.*}' | jq -r '.state // .data.node.state')
            
            echo "Current Status: $STATUS"
            if [ "$STATUS" == "FAILED" ]; then exit 1; fi
            if [ "$STATUS" != "SUCCEEDED" ]; then sleep 30; fi
          done

      - name: 🔍 Generate Mannequin CSV
        run: |
          pip install requests
          python3 scripts/05-generate_mannequins.py

      - name: 📤 Upload CSV
        uses: actions/upload-artifact@v4
        with:
          name: mannequins-to-edit
          path: mannequins.csv
